
Postgresql_client can work with this specific database.

The structure of the datavase is a follows
CREATE TABLE software_info (software_name varchar(50), description varchar(255) default 'Неизвестно', size bigint check(size > 0), category varchar(50) default 'неизвестно', price money, PRIMARY KEY (software_name));
CREATE TABLE software_and_developers (email varchar(100) check(email like '%@%'), software_name varchar(50), PRIMARY KEY (email, software_name));
CREATE TABLE developers (email varchar(100) check(email like '%@%'), fio varchar(150), organization varchar(150), PRIMARY KEY (email));
CREATE TABLE depend (software_name varchar(50), pkg_name varchar(150), PRIMARY KEY (software_name, pkg_name));
CREATE TABLE packages ( pkg_name varchar(150), PRIMARY KEY (pkg_name)); 
CREATE TABLE order_pkg (software_name varchar(50), pkg_location varchar(150), PRIMARY KEY (software_name, pkg_location));
CREATE TABLE methods (pkg_location varchar(150), type_location varchar(50), PRIMARY KEY (pkg_location));


